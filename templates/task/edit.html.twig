{% extends 'base.html.twig' %}

{% block title %}TodoList{% endblock %}

{% block body %}

    <div class="pt-3">
        <h1 class="text-center">Editer une tâche</h1>
    </div>
    <div class="container mt-4">
        {{ form_start(form) }}
        {{ form_rest(form) }}
        <div class="pt-3">
            <button class="btn btn-primary">Editer</button>
            <a href="{{ path('tasks', {id: task.todoList.id}) }}">Retour</a>
        </div>

        {{ form_end(form) }}
    </div>

{% endblock %}